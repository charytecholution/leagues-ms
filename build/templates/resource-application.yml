spring:
  application:
    name: <name>
  profiles:
    active: local    

server:
  port: <port_name>


logging:
  level:
    org.springframework.security: DEBUG
    log4j.logger.org.hibernate.SQL: DEBUG
    com.makeurpicks: DEBUG